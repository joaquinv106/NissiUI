import { Box, Flex, Stack, Text } from "@chakra-ui/react"
import { Circle } from "lucide-react"

import type { NActivityTimelineProps } from "./types"

export function NActivityTimeline({ items, compact = false, "aria-label": ariaLabel = "Actividad", unstyled = false, classNames, styles }: NActivityTimelineProps) {
  return <Stack as="ol" aria-label={ariaLabel} listStyleType="none" m="0" p="0" gap="0" className={classNames?.root} css={styles?.root} data-scope="n-activity-timeline" data-part="root">{items.map((item, index) => <Flex as="li" key={item.id} gap="3" position="relative" pb={index === items.length - 1 ? "0" : compact ? "3" : "5"} className={classNames?.item} css={styles?.item} data-part="item"><Flex width="8" justify="center" flexShrink="0"><Flex zIndex="1" bg={unstyled ? undefined : "bg.panel"} color={`${item.colorPalette ?? "blue"}.fg`} rounded={unstyled ? undefined : "full"} boxSize="8" align="center" justify="center" className={classNames?.marker} css={styles?.marker} data-part="marker">{item.icon ?? <Circle size={12} fill="currentColor" />}</Flex>{index < items.length - 1 ? <Box position="absolute" top="8" bottom="0" insetInlineStart="calc(1rem - 1px)" borderInlineStartWidth={unstyled ? undefined : "2px"} borderColor="border" /> : null}</Flex><Stack gap="1" minW="0" pt="1" className={classNames?.content} css={styles?.content} data-part="content"><Flex gap="2" direction={{ base: "column", sm: "row" }} justify="space-between"><Text fontWeight="semibold">{item.title}</Text>{item.timestamp ? <Text textStyle="xs" color="fg.muted">{item.timestamp}</Text> : null}</Flex>{item.description ? <Text color="fg.muted" textStyle="sm">{item.description}</Text> : null}{item.content}</Stack></Flex>)}</Stack>
}
