export {
  locationPath,
  isExternalRouteTarget,
  normalizeBasePath,
  normalizeRoutePath,
  parseRouteLocation,
  resolveRouteTarget,
  routeHref,
  stripBasePath,
} from "./location"
export { compileRouteBranches, defineNroutes, matchRoute, matchRoutes } from "./matcher"
