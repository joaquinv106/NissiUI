import type { NAppShellLabels } from "./types"

export const defaultNAppShellLabels: NAppShellLabels = {
  skipToContent: "Saltar al contenido principal",
  sidebarRegion: "Navegación de la aplicación",
  contentRegion: "Contenido principal",
}
